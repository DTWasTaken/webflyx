This repo was created in the boot.dev git training course.

It mostly just covered basic git usage (commits, merges, rebases, pushes, etc) and used the letter in the commit messages to verify and check progess.
